<div style="padding: 8rem var(--gutter) 6rem; max-width: 780px; margin: 0 auto;">
  <h1 style="font-family: var(--font-serif); font-size: clamp(2rem,4vw,3rem); font-weight: 300; margin-bottom: 2.5rem;">{{ page.title }}</h1>
  <div class="rte" style="font-size: 0.92rem; line-height: 1.85; color: var(--color-mid);">
    {{ page.content }}
  </div>
</div>
